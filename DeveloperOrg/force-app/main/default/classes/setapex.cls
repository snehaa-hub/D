public class setapex {
        public static void myMethod(){
            set<Integer> setList = new set<Integer>{10,100,10,50,30};
       
           system.debug('setList:'+setList);
  for (Integer x : setList){
               system.debug(x);
        }
}
        }